FROM golang:1.25-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /dev-auth ./cmd/dev-auth

FROM alpine:3.19
RUN apk add --no-cache ca-certificates
COPY --from=builder /dev-auth /dev-auth
EXPOSE 8090
CMD ["/dev-auth"]
